---
alertmanager:
  interval: 60s
  servers:
    - name: home
      uri: http://vmsingle-victoria-metrics.observability.svc.cluster.local:8428
      timeout: 10s
      healthcheck:
        visible: false
        filters:
          alertmanager:
            - alertname=Watchdog
            - prometheus=observability/victoria-metrics
