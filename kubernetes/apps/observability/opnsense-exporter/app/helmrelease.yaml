---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app opnsense-exporter
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: opnsense-exporter
  values:
    controllers:
      opnsense-exporter:
        containers:
          app:
            image:
              repository: ghcr.io/athennamind/opnsense-exporter
              tag: 0.0.13@sha256:04b3bca486b299e2138e9df380c71e9499db6ce62c08ec62698239aacc9d85bf
            args:
              # - --log.level=debug
              # - --log.format=json
              - --web.listen-address=:8080
            env:
              OPNSENSE_EXPORTER_INSTANCE_LABEL: "gw"
              OPNSENSE_EXPORTER_DISABLE_OPENVPN: "true"
              OPNSENSE_EXPORTER_OPS_API: "192.168.1.1"
              OPNSENSE_EXPORTER_OPS_INSECURE: true
              OPNSENSE_EXPORTER_OPS_PROTOCOL: https
            envFrom: &envFrom
              - secretRef:
                  name: opnsense-exporter-secret
            probes:
              liveness:
                enabled: true
              readiness:
                enabled: true
            resources: &resources
              limits:
                memory: 128Mi
              requests:
                cpu: 10m
            securityContext: &securityContext
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL
      check-firmware-update:
        type: cronjob
        cronjob:
          schedule: "@daily"
          failedJobsHistory: 1
          successfulJobsHistory: 0
          timeZone: "America/Los_Angeles"
        containers:
          app:
            image:
              repository: docker.io/alpine/curl
              tag: 8.17.0@sha256:011af24177b6451b2bfab330ce36c7d3881a7174c5985affec2851824f9bd2b4
            args:
              - "--fail"
              - "--silent"
              - "--show-error"
              - "--location"
              - "--insecure"
              - "--request"
              - POST
              - "--user"
              - >-
                $(OPNSENSE_EXPORTER_OPS_API_KEY):$(OPNSENSE_EXPORTER_OPS_API_SECRET)
              - "--data"
              - "{}"
              - https://opnsense.home.arpa/api/core/firmware/status
            envFrom: *envFrom
            resources: *resources
            securityContext: *securityContext
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
    service:
      app:
        controller: *app
        ports:
          http:
            port: 8080
    serviceMonitor:
      app:
        endpoints:
          - port: http
            scheme: http
            path: /metrics
            interval: 1m
            scrapeTimeout: 10s
