---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: plex-auto-languages-secret
spec:
  target:
    template:
      type: Opaque
      data:
        PLEX_TOKEN: "{{ .PLEX_TOKEN }}"
  data:
    - secretKey: PLEX_TOKEN
      sourceRef:
        storeRef:
          name: bitwarden-fields
          kind: ClusterSecretStore
      remoteRef:
        key: 0fc8bc10-24e6-4e4b-b1d9-aaec001fd8fb  # Plex
        property: PLEX_TOKEN
