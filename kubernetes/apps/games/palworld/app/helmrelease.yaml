---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2beta2.json
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: palworld
spec:
  interval: 30m
  chart:
    spec:
      chart: app-template
      version: 2.5.0
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  dependsOn:
    - name: longhorn
      namespace: storage
    - name: volsync
      namespace: storage
  values:
    controllers:
      main:
        containers:
          main:
            image:
              repository: ghcr.io/jammsen/docker-palworld-dedicated-server
              tag: master@sha256:2ac5ffc0f24d041821c02d639693e33c95989b47b3beb0f1f1bba7d0d189bef1
            env:
              SERVER_NAME: "Perry"
              SERVER_DESCRIPTION: "Perry's Server"
              COMMUNITY_SERVER: false
            envFrom:
              - secretRef:
                  name: palworld-secret
            probes:
              liveness: &probes
                enabled: false
              readiness: *probes
              startup: *probes
            resources:
              limits:
                memory: 24Gi
              requests:
                cpu: 500m
            securityContext:
              allowPrivilegeEscalation: false
              capabilities: { drop: ["ALL"] }
        pod:
          automountServiceAccountToken: false
          securityContext:
            fsGroup: 1000
            fsGroupChangePolicy: OnRootMismatch
            runAsGroup: 1000
            runAsNonRoot: true
            runAsUser: 1000
            seccompProfile: { type: RuntimeDefault }
    persistence:
      config:
        enabled: true
        existingClaim: palworld
        globalMounts:
          - path: /palworld
      tmp:
        type: emptyDir
    service:
      main:
        annotations:
          io.cilium/lb-ipam-ips: 192.168.1.252
        ports:
          http:
            enabled: false
          game:
            protocol: UDP
            port: 8211
          rcon:
            port: 25575
        type: LoadBalancer
