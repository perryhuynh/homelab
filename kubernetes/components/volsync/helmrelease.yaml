---
# yaml-language-server: $schema=https://raw.githubusercontent.com/bjw-s-labs/helm-charts/main/charts/other/app-template/schemas/helmrelease-helm-v2.schema.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: volsync-unlock-${APP}
spec:
  interval: 1h
  chartRef:
    kind: OCIRepository
    name: volsync-unlock-${APP}
  values:
    controllers:
      restic:
        defaultContainerOptionsStrategy: merge
        defaultContainerOptions:
          image:
            repository: mirror.gcr.io/restic/restic
            tag: 0.19.0@sha256:7f44e0057b82348597568ea209360762d0b38f8e1dbc8ad859661ac1055e45f2
          envFrom:
            - secretRef:
                name: "${APP}-volsync-secret"
          resources:
            limits:
              memory: 256Mi
            requests:
              cpu: 10m
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - "ALL"
        type: cronjob
        cronjob:
          schedule: "* * 31 2 *"
          concurrencyPolicy: Forbid
          failedJobsHistory: 1
          successfulJobsHistory: 0
          suspend: true
          timeZone: &timeZone America/Los_Angeles
        initContainers:
          list:
            args:
              - "snapshots"
            env:
              TZ: *timeZone
        containers:
          app:
            args:
              - "unlock"
              - "--remove-all"
    defaultPodOptions:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
    persistence:
      repository:
        type: nfs
        path: /mnt/tank/volsync
        server: nas.home.arpa
      tmpfs:
        type: emptyDir
        globalMounts:
          - path: /.cache
            subPath: cache
